# Installer-Manifest (winget). Platzhalter <...> vor dem Einreichen ersetzen.
# Schema: https://aka.ms/winget-manifest.installer.1.6.0.schema.json
PackageIdentifier: edgebird-lab.RAG-Lernsystem
PackageVersion: "<VERSION>"
InstallerType: inno                    # Setup.exe ist ein Inno-Setup-Installer
Scope: user                            # installiert nach %LOCALAPPDATA% (kein Admin)
InstallModes:
  - interactive
  - silent
  - silentWithProgress
Installers:
  - Architecture: x64
    InstallerUrl: <URL-ZUR-RELEASE-SETUP.EXE>   # z. B. https://github.com/edgebird-lab/RAG_System/releases/download/v<VERSION>/RAG-Lernsystem-Setup.exe
    InstallerSha256: <SHA256>          # aus installer\SHA256SUMS.txt (GROSSBUCHSTABEN)
ManifestType: installer
ManifestVersion: 1.6.0
